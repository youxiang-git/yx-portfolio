import React from "react";

const Loading = () => {
  return (
    <div className="z-100 h-[100vh] w-[100vw] bg-white text-[100vh]">
      <p>Loading...</p>
    </div>
  );
};

export default Loading;
